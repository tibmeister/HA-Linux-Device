# MQTT Configuration
[MQTT]
broker = mqtt.example.com
port = 1883
topic = sensor/data
client_id = mqtt-client-001
username = user
password = password

# Application Settings
[App]
log_level = info
interval = 5  # Seconds between data transmissions
